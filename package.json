{
  "name": "mcp-connector",
  "version": "0.2.0",
  "main": "main.js",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=neutral --outfile=mcp-connector/main.js --external:obsidian --external:http --external:https --external:net --format=cjs && esbuild src/tools/mcp-http-ws-bridge.js --bundle --platform=node --outfile=mcp-connector/mcp-http-ws-bridge.js && node -e \"const fs = require('fs'); fs.copyFileSync('manifest.json', 'mcp-connector/manifest.json'); fs.copyFileSync('src/tools/start-bridge.sh', 'mcp-connector/start-bridge.sh'); fs.copyFileSync('src/tools/start-bridge.cmd', 'mcp-connector/start-bridge.cmd');\"",
    "watch": "esbuild src/main.ts --bundle --platform=neutral --outfile=main.js --external:obsidian --external:http --external:https --external:net --watch",
    "test": "jest",
    "lint": "echo 'Add linter if desired'"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^18.0.0",
    "esbuild": "^0.18.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.2.0"
  }
}
