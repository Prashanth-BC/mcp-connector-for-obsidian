
{
  "name": "obsidian-connector",
  "version": "0.2.0",
  "main": "main.js",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=main.js --external:obsidian && esbuild src/mcp-server.ts --bundle --platform=node --outfile=mcp-server.js --external:obsidian",
    "watch": "esbuild src/main.ts --bundle --platform=node --outfile=main.js --external:obsidian --watch",
    "test": "jest",
    "lint": "echo 'Add linter if desired'"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "typescript": "^5.2.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^18.0.0"
  }
}
